requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
PyYAML>=6.0
gspread>=6.0
playwright>=1.45      # used only for shops with `fetcher: playwright`
anthropic>=0.40       # used only when ANTHROPIC_API_KEY is set (LLM fallback extraction)
pytest>=8.0
